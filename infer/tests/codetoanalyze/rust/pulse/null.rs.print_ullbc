# Final ULLBC before serialization:

#[lang_item("ptr_null")]
pub fn core::ptr::null::<i32>() -> *const i32
{
    let @0: *const i32; // return
    let @1: *const i32; // anonymous local

    bb0: {
        storage_live(@1);
        @1 := cast<usize, *const i32>(const (0 : usize));
        @0 := move (@1);
        return;
    }
}

// Full name: null::main
fn main()
{
    let @0: (); // return
    let ptr@1: *const i32; // local
    let x@2: i32; // local

    bb0: {
        storage_live(ptr@1);
        ptr@1 := core::ptr::null::<i32>() -> bb1 (unwind: bb2);
    }

    bb1: {
        storage_live(x@2);
        x@2 := copy (*(ptr@1));
        @0 := ();
        storage_dead(x@2);
        storage_dead(ptr@1);
        @0 := ();
        return;
    }

    bb2: {
        unwind_continue;
    }
}



